export type Props = object;

export default async function Page({}: Props) {
    return (
        <div>
            <main>
                <h1>Profile</h1>
            </main>
        </div>
    );
}
